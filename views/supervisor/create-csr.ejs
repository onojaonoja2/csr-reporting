<%- include('../partials/header') %>
<div class="flex">
  <%- include('../partials/sidebar') %>
  <main class="flex-1 p-6">
    <h2 class="text-2xl font-bold text-brand mb-6">Create CSR</h2>
    <% if (error) { %>
      <div class="bg-red-50 dark:bg-red-900/30 text-red-700 dark:text-red-300 p-3 rounded-lg mb-4 text-sm"><%= error %></div>
    <% } %>
    <div class="card max-w-lg">
      <form action="/supervisor/csr/create" method="POST" class="space-y-4">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div>
          <label class="block text-sm font-medium mb-1">Full Name</label>
          <input type="text" name="fullName" class="form-input" required placeholder="Enter full name">
        </div>
        <div>
          <label class="block text-sm font-medium mb-1">Email</label>
          <input type="email" name="email" class="form-input" required placeholder="Enter email">
        </div>
        <div>
          <label class="block text-sm font-medium mb-1">Password</label>
          <input type="password" name="password" class="form-input" required placeholder="Enter password">
        </div>
        <div>
          <label class="block text-sm font-medium mb-1">Phone Number</label>
          <input type="text" name="phoneNumber" class="form-input" placeholder="Enter phone number">
        </div>
        <div>
          <label class="block text-sm font-medium mb-1">Address</label>
          <input type="text" name="address" class="form-input" placeholder="Enter address">
        </div>
        <div>
          <label class="block text-sm font-medium mb-1">Zone</label>
          <select name="zone" class="form-input" id="zoneSelect">
            <option value="">Select Zone</option>
            <% Object.keys(zones).forEach(z => { %>
              <option value="<%= z %>"><%= z %></option>
            <% }) %>
          </select>
        </div>
        <div>
          <label class="block text-sm font-medium mb-1">State</label>
          <select name="state" class="form-input" id="stateSelect">
            <option value="">Select State</option>
          </select>
        </div>
        <div>
          <label class="block text-sm font-medium mb-1">LGA</label>
          <select name="lga" class="form-input" id="lgaSelect">
            <option value="">Select LGA</option>
          </select>
        </div>
        <div class="flex gap-3">
          <button type="submit" class="btn-primary">Create CSR</button>
          <a href="/supervisor/dashboard" class="px-4 py-2 border rounded-lg text-sm hover:bg-surface">Cancel</a>
        </div>
      </form>
    </div>
  </main>
</div>
<script>
const zones = <%- JSON.stringify(zones) %>;
document.getElementById('zoneSelect').addEventListener('change', function() {
  const states = zones[this.value] || {};
  const stateSelect = document.getElementById('stateSelect');
  stateSelect.innerHTML = '<option value="">Select State</option>';
  Object.keys(states).forEach(s => { stateSelect.innerHTML += '<option value="' + s + '">' + s + '</option>'; });
  document.getElementById('lgaSelect').innerHTML = '<option value="">Select LGA</option>';
});
document.getElementById('stateSelect').addEventListener('change', function() {
  const zone = document.getElementById('zoneSelect').value;
  const lgas = (zones[zone] || {})[this.value] || [];
  const lgaSelect = document.getElementById('lgaSelect');
  lgaSelect.innerHTML = '<option value="">Select LGA</option>';
  lgas.forEach(l => { lgaSelect.innerHTML += '<option value="' + l + '">' + l + '</option>'; });
});
</script>
<%- include('../partials/footer') %>
